# Web Tests

These tests cover the semantic web subsystem:
- cache behavior
- schema validation
- Ollama client behavior
- Playwright/page loading behavior
- graph fallback behavior
- scrape service caching
